<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.imageplatform.mapper.UserMapper">

    <!-- 这就是 resultMap：定义映射规则 -->
    <resultMap id="userResultMap" type="com.imageplatform.entity.User">
        <id property="id" column="id"/>
        <result property="avatarUrl" column="avatar_url"/>
    </resultMap>

    <!-- 使用定义好的 resultMap -->
    <select id="findById" resultMap="userResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="userResultMap">
        SELECT * FROM users
    </select>

    <!-- 插入时 MyBatis 会自动根据属性名找对应的参数 -->
    <insert id="insert" parameterType="com.imageplatform.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, avatar_url)
        VALUES (#{username}, #{password}, #{email}, #{avatarUrl})
    </insert>
</mapper>